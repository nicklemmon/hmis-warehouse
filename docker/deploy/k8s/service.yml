apiVersion: v1
kind: Service
metadata:
  name: "warehouse-web-service"
  namespace: ${NAMESPACE}
spec:
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  type: NodePort
  selector:
    app: "warehouse-web"
